services:
  - type: web
    runtime: python
    name: buddy-allocation-api
    buildCommand: apt-get update && apt-get install -y gcc && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18